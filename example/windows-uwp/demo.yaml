# 生命周期 demo：把所有 TUN 流量交给宿主自行启动的回环 SOCKS5 服务。
# 没有监听在 127.0.0.1:1080 的 SOCKS5 服务时，VPN 仍可连接，但业务流量会失败。
tun:
  enable: true
  mtu: 1500

proxies:
  - name: local-socks
    type: socks5
    server: 127.0.0.1
    port: 1080
    udp: true

dns:
  enable: false

rules:
  - MATCH,local-socks
